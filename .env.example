# ========================================
# Budgeify v2.0 "Sovereign" - Environment Variables
# ========================================
#
# 🎓 MENTOR NOTU - Environment Variables:
# --------------------------------------
# Environment variables, hassas bilgileri (API keys, DB credentials)
# kod dışında saklamak için kullanılır.
#
# .env.local → Geliştirme ortamı (git'e commit edilmez)
# .env.production → Production (Vercel'de ayarlanır)
#
# Vercel'de ayarlamak için:
# Project Settings → Environment Variables → Add
#
# NEVER commit .env.local to git!
# ========================================

# ----------------------------------------
# DATABASE - Neon PostgreSQL
# ----------------------------------------
# Neon Dashboard'dan alınır:
# 1. https://console.neon.tech adresine git
# 2. Proje oluştur veya seç
# 3. Connection Details → Connection String'i kopyala
#
# Format: postgresql://user:password@host/database?sslmode=require
DATABASE_URL=postgresql://username:password@ep-xxx.region.aws.neon.tech/budgeify?sslmode=require

# ----------------------------------------
# AUTHENTICATION - Clerk
# ----------------------------------------
# Clerk Dashboard'dan alınır:
# 1. https://dashboard.clerk.com adresine git
# 2. Uygulama oluştur veya seç
# 3. API Keys → Copy
#
# NEXT_PUBLIC_* → Client-side'da görünebilir (frontend)
# CLERK_SECRET_KEY → Sadece server-side'da kullanılır (backend)

# Clerk Publishable Key (Frontend)
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=pk_test_xxx

# Clerk Secret Key (Backend - ASLA EXPOSE ETME!)
CLERK_SECRET_KEY=sk_test_xxx

# Clerk Sign-in/Sign-up URLs
NEXT_PUBLIC_CLERK_SIGN_IN_URL=/sign-in
NEXT_PUBLIC_CLERK_SIGN_UP_URL=/sign-up
NEXT_PUBLIC_CLERK_AFTER_SIGN_IN_URL=/dashboard
NEXT_PUBLIC_CLERK_AFTER_SIGN_UP_URL=/dashboard

# Clerk Webhook Secret (Clerk Dashboard → Webhooks → Signing Secret)
# Webhook'ların gerçekten Clerk'ten geldiğini doğrular
CLERK_WEBHOOK_SECRET=whsec_xxx

# ----------------------------------------
# AI - Oracle (Opsiyonel, Phase 5'te eklenecek)
# ----------------------------------------
# Anthropic API key (Claude için)
# https://console.anthropic.com/account/keys
# ANTHROPIC_API_KEY=sk-ant-xxx

# ----------------------------------------
# VERCEL CRON
# ----------------------------------------
# Secret token to authenticate Vercel Cron requests
# Generate with: openssl rand -hex 32
# Set in Vercel Project Settings → Environment Variables
CRON_SECRET=your-cron-secret-here

# ----------------------------------------
# APP CONFIG
# ----------------------------------------
# Application URL (Vercel'de otomatik ayarlanır)
NEXT_PUBLIC_APP_URL=http://localhost:3000
NEXT_PUBLIC_APP_NAME=Budgeify

# Node environment
NODE_ENV=development

# ----------------------------------------
# OPTIONAL - Analytics & Monitoring
# ----------------------------------------
# Google Analytics
# NEXT_PUBLIC_GA_ID=G-XXXXXXXXXX

# Sentry Error Tracking
# SENTRY_DSN=https://xxxxx@xxxxx.ingest.sentry.io/xxxxx

# Feature Flags
# NEXT_PUBLIC_ENABLE_PWA=true
# Oracle AI (OracleHero, BrainCard, InsightCard, AIAssistant, DesktopAICard)
# Set to "1" to enable (desktop-only, hidden on mobile for performance), anything else or undefined = disabled
# NEXT_PUBLIC_FEATURE_ORACLE=1
